#==============SELECT SALARY DEVELOPERS============================

SELECT SALARY, PROJECT FROM DEVELOPERS
INNER JOIN DEVELOPERS_PROJECTS ON DEVELOPERS.ID = ID_DEVELOPERS
INNER JOIN PROJECTS on ID_PROJECTS = PROJECTS.ID
GROUP BY PROJECT
ORDER BY SALARY DESC;