#==============SELECT SUM SALARY DEVELOPERS========================
SELECT SUM(SALARY) AS TOTAL FROM DEVELOPERS
  INNER JOIN DEVELOPERS_SKILLS ON DEVELOPERS.ID = DEVELOPERS_SKILLS.ID_DEVELOPERS
  INNER JOIN SKILLS ON DEVELOPERS_SKILLS.ID_SKILLS = SKILLS.ID
WHERE SKILL LIKE  '%JAVA%';